╔══════════════════════════════════════════════════════════════════════════════╗
║                    CITY-STATE LOOKUP SYSTEM - CHEAT SHEET                   ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│ 🚀 QUICK COMMANDS                                                            │
└──────────────────────────────────────────────────────────────────────────────┘

▸ Process new data with unknown cities (FREE):
  python auto_update_mapping.py new_data.csv

▸ Preview changes without saving:
  python auto_update_mapping.py new_data.csv --dry-run

▸ Use Google API for better accuracy:
  python auto_update_mapping.py data.csv --provider google --api-key YOUR_KEY

▸ Start REST API server:
  python city_state_api.py

▸ Regenerate mapping from complete dataset:
  python generate_city_state_mapping.py us_concerts_with_states.csv


┌──────────────────────────────────────────────────────────────────────────────┐
│ 🔍 API QUERIES                                                               │
└──────────────────────────────────────────────────────────────────────────────┘

▸ Look up single city:
  curl http://localhost:5000/api/state/Las%20Vegas

▸ Get all cities in state:
  curl http://localhost:5000/api/cities/California

▸ Search for cities:
  curl "http://localhost:5000/api/search?q=san"

▸ Batch lookup:
  curl -X POST http://localhost:5000/api/lookup \
    -H "Content-Type: application/json" \
    -d '{"cities": ["New York", "Chicago"]}'

▸ Get statistics:
  curl http://localhost:5000/api/stats


┌──────────────────────────────────────────────────────────────────────────────┐
│ 💻 PYTHON CODE                                                               │
└──────────────────────────────────────────────────────────────────────────────┘

▸ Local database lookup (373 cities):
  from city_state_mapping import get_state
  state = get_state('Las Vegas')

▸ External API lookup (ANY city):
  from external_city_lookup import get_state_from_api
  state = get_state_from_api('Bozeman')  # Queries OpenStreetMap

▸ Batch lookup:
  from external_city_lookup import batch_lookup_cities
  results = batch_lookup_cities(['City1', 'City2', 'City3'])

▸ Update existing mapping:
  from external_city_lookup import update_mapping_with_new_cities
  import json
  
  with open('city_state_mapping.json', 'r') as f:
      mapping = json.load(f)
  
  new = ['Bozeman', 'Anchorage']
  updated = update_mapping_with_new_cities(mapping, new)


┌──────────────────────────────────────────────────────────────────────────────┐
│ 🌐 API PROVIDERS                                                             │
└──────────────────────────────────────────────────────────────────────────────┘

  Provider    │ Cost        │ Speed      │ API Key? │ Best For
  ────────────┼─────────────┼────────────┼──────────┼─────────────────────
  Nominatim   │ FREE        │ 1 req/sec  │ No       │ Batch processing
  Google      │ $5/1000*    │ Very fast  │ Yes      │ Production/accuracy
  MapBox      │ $0.50/1000* │ Fast       │ Yes      │ Cost-effective
  
  * After free tier


┌──────────────────────────────────────────────────────────────────────────────┐
│ 📊 DATABASE STATS                                                            │
└──────────────────────────────────────────────────────────────────────────────┘

  • 373 cities mapped
  • 50 US states covered
  • 7,776 concert events analyzed
  • $16.9B total revenue
  • 140.2M tickets sold


┌──────────────────────────────────────────────────────────────────────────────┐
│ 📁 KEY FILES                                                                 │
└──────────────────────────────────────────────────────────────────────────────┘

  auto_update_mapping.py       ➜ Process new data automatically
  external_city_lookup.py      ➜ Core external API module
  city_state_api.py            ➜ REST API server
  city_state_mapping.json      ➜ Database (373 cities)
  city_state_mapping.py        ➜ Python module
  api_demo.html                ➜ Interactive web demo


┌──────────────────────────────────────────────────────────────────────────────┐
│ 📖 DOCUMENTATION                                                             │
└──────────────────────────────────────────────────────────────────────────────┘

  INDEX.md                     ➜ Complete package overview
  QUICK_START.md               ➜ 5-minute getting started
  EXTERNAL_API_GUIDE.md        ➜ External API integration
  API_DOCUMENTATION.md         ➜ REST API reference
  US_Cities_Capacity_Report    ➜ 393 cities analysis
  US_States_Complete_Report    ➜ 50 states analysis


┌──────────────────────────────────────────────────────────────────────────────┐
│ 🎯 DECISION TREE                                                             │
└──────────────────────────────────────────────────────────────────────────────┘

  Got new data with unknown cities?
  └─➜ python auto_update_mapping.py data.csv

  Need web service for app?
  └─➜ python city_state_api.py

  Python script needs city lookup?
  └─➜ from city_state_mapping import get_state

  Need to query city you've never seen?
  └─➜ from external_city_lookup import get_state_from_api


┌──────────────────────────────────────────────────────────────────────────────┐
│ 🐛 TROUBLESHOOTING                                                           │
└──────────────────────────────────────────────────────────────────────────────┘

  "City not found"
  ➜ Try external API: get_state_from_api('CityName')

  "Module not found"
  ➜ pip install flask flask-cors requests

  "Rate limit exceeded"
  ➜ Using Nominatim? Wait 1 sec between requests
     Or upgrade to Google/MapBox

  "API key invalid"
  ➜ Check: console.cloud.google.com or account.mapbox.com

  Want to preview changes?
  ➜ Add --dry-run flag to any command


┌──────────────────────────────────────────────────────────────────────────────┐
│ ⚡ ONE-LINERS                                                                 │
└──────────────────────────────────────────────────────────────────────────────┘

  # Look up city (Python)
  python -c "from external_city_lookup import get_state_from_api; print(get_state_from_api('Bozeman'))"

  # Process data and auto-discover states
  python auto_update_mapping.py new.csv && echo "✓ Done!"

  # Start API and test
  python city_state_api.py & sleep 2 && curl http://localhost:5000/api/state/Boston

  # Update mapping with 3 new cities
  python -c "from external_city_lookup import *; print(batch_lookup_cities(['City1','City2','City3']))"


╔══════════════════════════════════════════════════════════════════════════════╗
║  💡 TIP: Start with INDEX.md for complete system overview                   ║
╚══════════════════════════════════════════════════════════════════════════════╝
